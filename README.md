# Тестовый проект: "Список счетчиков"

## Доступные команды:

- yarn dev (npm run dev) - Запуск проекта в режиме разработки
- yarn build (npm run build) - Сборка проекта
- yarn preview (npm run preview) - Просмотр собранного проекта
